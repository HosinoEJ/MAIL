# 部署模式: 'vercel' 或留空(默认VPS模式)
DEPLOY_MODE=

# 服务器端口
PORT=3000

# SMTP配置
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-password-or-app-specific-password
FROM_NAME=Your Name

# 接收目标邮箱的API地址
NEW_EMAIL_API=https://example.com/api/new-email
